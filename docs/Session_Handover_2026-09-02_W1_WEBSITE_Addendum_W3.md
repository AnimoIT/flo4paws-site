# Addendum to Session_Handover_2026-09-02_WEBSITE.md — written 2026-09-04 (W3)

Read this alongside the W1 handover. It does not replace it. Each line below corrects or supersedes a specific claim; everything else in W1 stands and was re-measured on 2026-09-04 (18/18 recon cells).

## Corrections

| W1 section | W1 said | Correction | Basis |
|---|---|---|---|
| §1, §11 | Site generated by `gen.py` + `content.py` | True, but the generator was built in the Claude sandbox and **never presented as a download**. It does not exist. Reconstructed in W3 as `src/gen.py` + `src/pages/*.html` in `AnimoIT/flo4paws-site`, proven byte-identical. | 0 `.py` in all 7 build tarballs; `find /` empty |
| §3 | "`/healthform` — uncomment at cutover" | No commented-out `location` exists; only a prose comment at vhost line 49. Write the directive fresh at cutover. | W2 finding, re-measured W3 |
| §3 | v7 bundle == what is served | **Superseded by W2** (2 Sep 19:01): footer wording, hamburger nav, `site.js`, CSS. v7 is provenance only. The served tree is tagged `w2-live` in the repo. | `diff -rq` 24 files differ |
| §3 | Meta files at `/root/incoming/flo4paws-site-meta/` are the vhost | The committed/meta vhost was **pre-certbot** (3382 B); the live one (3668 B) has certbot's 443 + redirect blocks and W2's comment rewording. `deploy/nginx-flo4paws.conf` now equals live (PR #2). | `diff` |
| §4 cutover gate | 4 items | Ian (W3, verbal): **BL-576 done; one real end-to-end submission done; BL-566 parked**. Only cross-domain GA4 + Consent Mode remains, and it is a launch-day task done *before* the `www` A record moves (Ads conversion continuity). Neither closure is yet in any register — WBL-651. | Ian |
| §6 | Site content owed by Flo | Still owed: Newgrange line (still rendering as client-facing text), verbatim Trustpilot reviews, partner logos ×3, credential badges ×4, affiliate check, 12-page read-through, Lincolnshire ruling. **Plus now**: read-through of `/about/`. | W3 grep + PR #3 |
| §8 / `ia-v2.md` §6 | Ads spend £634.93 | Wrong (subtotal rows summed). W1 §2 has the corrected figure. `ia-v2.md` still carries the wrong number and should be corrected in the repo when it is moved there (WBL-650). | W1 self-correction |
| §9 controls | Lily control `app.lilyvetphysiotherapy.co.uk` (implied) | Host is **`.com`**. | vhost `server_name` |
| §9 controls | PM2 count 5 | Confirmed 5 — memory summary saying 4 is the stale one. | `pm2 jlist` |

## Not corrections, but W1 should have said

- The vhost's `location ~* \.(jpg|…)$` and `location = /site.css` blocks use `add_header`, which **replaces** the server-level `add_header` set inside those locations — `X-Robots-Tag` is not emitted on images/css. Harmless now; matters when security headers are added (repeat them or `include` a snippet).
- HTML pages carry **no** Cache-Control (heuristic caching). Not a blocker; `no-cache` is a pending ruling (WBL-645).
- The `beerscope.co.uk` vhost shares this nginx and its reload blast radius; add to the control list.
